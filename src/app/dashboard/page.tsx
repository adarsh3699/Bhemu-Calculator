import DashboardView from "@/components/Dashboard/DashboardView";

export const metadata = {
	title: "Dashboard | Bhemu Calculator",
	description: "Your academic overview — CGPA stats, semester performance chart, roadmap, and recent subjects at a glance.",
};

export default function DashboardPage() {
	return <DashboardView />;
}
