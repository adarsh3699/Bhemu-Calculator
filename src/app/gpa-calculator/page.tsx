import GpaCalculatorView from "@/components/GpaCalculator/GpaCalculatorView";

export const metadata = {
	title: "GPA Calculator | Bhemu",
	description: "Calculate your semester GPA and cumulative CGPA in a clean, real-time shared workspace.",
};

export default function GpaCalculatorPage() {
	return <GpaCalculatorView />;
}
